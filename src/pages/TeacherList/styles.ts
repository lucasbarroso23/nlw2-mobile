import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        color: '#f0f0f7'
    },

    teacherList: {
        marginTop: -60,
    }
})

export default styles;